# GateDesk 远程桌面控制系统客户端集成设计方案 1.2

## 1. 背景与目标

GateDesk 基于 RustDesk 客户端构建，用于实现用户端与运维端之间的远程协助。一次协助由两个参与方组成：

- **用户端（受控方）**：运行用户网页和 GateDesk 原生客户端。
- **运维端（控制方）**：运行运维网页和 GateDesk 原生客户端。

GateDesk 服务端负责网页登录、客户端注册与配对、文字聊天中继、控制信令、会话生命周期和审计；RustDesk 服务器负责底层设备发现、打洞和中继。

### 1.1 目标

实现以下完整闭环：

1. 用户端和运维端登录。
2. 两端 GateDesk 客户端注册并完成配对。
3. 两端网页进行文字聊天。
4. 运维端一键发起远程控制，使用原生 GateDesk 会话传输音频。
5. 任一端结束会话后，另一端收到通知并同步关闭。

### 1.2 范围

本方案覆盖：

- 客户端集成方式；
- GateDesk 本地 API；
- GateDesk 服务端接口与会话状态；
- 启动、配对、聊天、控制和结束流程；
- 安全边界、权限和异常处理；
- PoC 实施顺序与待验证事项。

本方案不包含 RustDesk 服务器的部署和改造。

### 1.3 需求澄清与迭代（2026-09-04）

角色定义：

- **用户端 = 客户**（受控方）：运行客户网页 + GateDesk 原生客户端。
- **运维端 = 公司内部运维人员**（控制方）：运行运维网页 + GateDesk 原生客户端。
- **超级管理员**：具备「强制控制」能力的运维人员，可不经客户请求直接接管其桌面。

本轮迭代确定的需求：

1. 角色明确：运维端为公司内部运维人员，用户端为客户。
2. 两端可进行简单文字对话。
3. 用户端 GateDesk 启动成功后，网页自动获取本机 GateDesk ID 并上报运维端；用户端界面同时显示自己的 ID。
4. 用户端提供「请求协助」按钮。
5. 运维端收到「请求协助」后，点击「开始协助」即进入远程桌面协助流程。
6. 全流程无需手填 GateDesk ID。
7. 额外分支：超级管理员可强制控制用户端远程桌面（无需客户点「请求协助」）。

由此，服务端模型由「会话中心」调整为「设备中心」（见 5.3），网页不再出现手动填写 ID 的入口。

## 2. 组件与职责

| 组件 | 部署位置 | 主要职责 |
|------|----------|----------|
| 用户网页 | 用户端机器 | 登录、显示设备信息、聊天、会话状态展示 |
| 运维网页 | 运维端机器 | 登录、聊天、发起远程控制、会话状态展示 |
| GateDesk 客户端 | 用户端和运维端各一份 | 设备注册、远程桌面连接、音频传输、本地 API |
| GateDesk 服务端 | 服务端 | 登录、配对、聊天中继、控制信令、会话管理、审计 |
| RustDesk 服务器 | 服务端 | 设备发现、连接协商、直连失败时的中继 |

每个 GateDesk 客户端内嵌一个仅供本机访问的 HTTP API。网页通过该 API 获取设备 ID、设置密码、发起或结束原生连接。

## 3. 总体架构

```text
┌──────────────────────────────┐       ┌──────────────────────────────┐
│ 用户端机器                    │       │ 运维端机器                    │
│                              │       │                              │
│  用户网页 ── HTTP/WebSocket ──┼───────┼── HTTP/WebSocket ── 运维网页  │
│      │                       │       │                       │      │
│      │ 127.0.0.1:21120       │       │       127.0.0.1:21121 │      │
│      ▼                       │       │                       ▼      │
│  GateDesk（受控端）           │       │  GateDesk（控制端）           │
└──────────────┬───────────────┘       └──────────────┬───────────────┘
               │ 注册、连接协商和控制信令              │
               └────────────────┬─────────────────────┘
                                ▼
                    ┌────────────────────────┐
                    │ RustDesk 服务器         │
                    │ 设备发现与连接中继       │
                    └────────────────────────┘

用户网页/运维网页 ── HTTP/WebSocket ──▶ GateDesk 服务端
                                      登录、配对、聊天、信令、审计
```

同机运行两个客户端时，分别使用不同的 API 端口；分机运行时可统一使用默认端口。

## 4. GateDesk 本地 API

### 4.1 设计约束

1. 监听地址固定为 `127.0.0.1`，不得监听 `0.0.0.0` 或 `[::]`。
2. 端口可配置，默认使用 `21120`。
3. 每个请求都必须通过本地 API Token 校验。
4. 仅允许 JSON 请求和响应，并限制请求体大小。
5. 不返回或记录不必要的密码和 Token。
6. API 只提供网页集成所需的最小操作集。

### 4.2 端点

所有端点都需要通过 `X-GateDesk-Token` 请求头或 `token` 查询参数完成鉴权。

| 方法 | 路径 | 请求体 | 成功响应 | 用途 |
|------|------|--------|----------|------|
| GET | `/api/id` | 无 | `{"id":"279379655"}` | 获取本机设备 ID |
| GET | `/api/status` | 无 | `{"online":true,"in_session":false,"peer_id":null}` | 获取客户端状态 |
| POST | `/api/password` | `{"password":"..."}` | `{"ok":true}` | 设置受控端临时连接密码 |
| POST | `/api/connect` | `{"id":"...","password":"...","relay":false}` | `{"ok":true}` | 发起原生远程连接 |
| POST | `/api/disconnect` | 无 | `{"ok":true}` | 结束当前连接 |
| POST | `/api/voice` | `{"enabled":true}` | `{"ok":true}` | 启用或关闭语音 |

### 4.3 请求与响应规范

- GET 参数使用 query string；POST 参数使用 JSON body。
- 成功响应统一返回 `{"ok":true,...}`。
- 失败响应统一返回 `{"ok":false,"error":"..."}`。
- `Content-Type` 必须为 `application/json`。
- JSON 非法、参数缺失或参数类型错误返回 `400`。
- 未提供或校验失败返回 `401`。
- 路径不存在返回 `404`，方法不支持返回 `405`，内部错误返回 `500`。

### 4.4 Token 管理

- GateDesk 每次启动生成随机 Token。
- Token 保存于 GateDesk 应用目录下的 `api_token` 文件，文件权限为 `0600`。
- 启动脚本读取 Token 后，将其以短期启动参数传递给网页。
- Token 不写入普通日志，比较时使用常数时间比较。
- 生产环境应使用短时握手或一次性票据，避免长期将 Token 放在浏览器地址栏中。

### 4.5 客户端集成要点

- 本地 API 在 GateDesk 主进程完成初始化后启动。
- API 复用现有 TCP 监听能力，但 HTTP 请求解析、路由、JSON 编解码和错误处理需要单独实现。
- `/api/connect` 应复用 GateDesk 现有 URI Scheme/IPC 连接入口，避免重复实现连接逻辑。
- `/api/disconnect` 默认结束当前客户端的活动连接；如需只结束指定会话，应在实现阶段明确会话标识。
- 两个客户端必须使用隔离的配置目录或实例标识，避免 IPC、缓存和配置互相覆盖。

## 5. GateDesk 服务端设计

### 5.1 REST 接口

| 方法 | 路径 | 入参 | 说明 |
|------|------|------|------|
| POST | `/api/auth/login` | `{"role":"user\|operator\|superadmin"}` | 声明角色 |
| POST | `/api/device/register` | `{"id","password"}` | 用户端自动上线，上报本机 ID 与连接密码（幂等） |
| GET | `/api/devices` | 无 | 运维端列出全部设备及状态 |
| POST | `/api/request/create` | `{"deviceId"}` | 用户点「请求协助」：online→requested |
| POST | `/api/request/start` | `{"deviceId"}` | 运维点「开始协助」：requested→assisting，下发连接凭据 |
| POST | `/api/device/force-control` | `{"deviceId","role":"superadmin"}` | 超管强制控制：online→assisting，下发连接凭据 |
| POST | `/api/request/end` | `{"deviceId","by"}` | 结束协助：→online，通知对端 |

密码在服务端只保存加密结果（PoC 阶段为内存明文，见 5.3）。连接所需的明文凭据仅在控制请求成功时短时下发，并在会话结束后失效或轮换。

### 5.2 WebSocket

连接地址：`/ws?deviceId=...&role=user|operator|superadmin`

同一设备的 user 与 operator 同处一个房间，聊天与状态在此中继。

消息类型：

- `chat`：文字聊天；
- `control-requested`：已发起控制请求；
- `control-started`：控制连接已建立；
- `control-ended`：控制连接已结束；
- `peer-ended`：对端主动结束；
- `session-state`：会话状态变更。

服务端只向同一 `sessionId` 的合法参与方转发消息，并校验消息类型、大小和发送权限。

### 5.3 设备中心状态机

```text
online ──(request/create 客户点「请求协助」)──▶ requested
requested ──(request/start 运维点「开始协助」)──▶ assisting
online ──(device/force-control 超管「强制控制」)──▶ assisting
assisting ──(request/end 任一端结束)──▶ online
```

状态转换必须由服务端统一管理，客户端只能提交意图，不能直接修改设备状态。

### 5.4 强制控制（超级管理员）

- 超级管理员无需客户点「请求协助」，即可对任一在线（online）设备发起「强制控制」，直接进入远程桌面。
- 服务端校验请求方角色为 `superadmin` 才放行（PoC 阶段角色由前端声明、服务端弱校验，生产需接入真实鉴权）。
- 其余流程与「开始协助」一致：下发受控端 ID + 连接密码，运维端调本机 `/connect` 建立原生会话，并广播 `control-started`。

## 6. 核心业务流程

### 6.1 启动与配对

```text
1. 启动脚本启动 GateDesk。
2. GateDesk 初始化本地 API、生成 Token，并注册 RustDesk 服务器。
3. 启动脚本读取 Token，打开对应网页并附带 Token。
4. 用户网页自动调用本地 /api/id 获取本机 ID 并显示，随后设置随机连接密码并调用服务端 /api/device/register 自动上线（全程无需手填 ID）。
5. 运维网页登录（普通运维 / 超级管理员）后调用 /api/devices 轮询设备列表。
6. 用户点「请求协助」→ 设备状态变为 requested，运维端看到后点「开始协助」进入远程桌面。
```

### 6.2 文字聊天

两端网页分别建立 WebSocket 连接。聊天消息由服务端校验后在同一会话内转发，并附带消息 ID、发送方、时间戳和消息类型。

### 6.3 一键远程控制

```text
1. 运维端点击「开始协助」。
2. 运维网页调用 /api/request/start。
3. 服务端确认用户端在线且当前未被其他控制会话占用。
4. 服务端返回用户端设备 ID 和短时连接凭据。
5. 运维网页调用本机 /api/connect。
6. 运维端 GateDesk 通过原生连接入口建立控制会话。
7. 服务端广播 control-started，双方网页更新状态。
```

语音由原生 GateDesk 会话承载，不依赖网页音频通道。用户端需要授予屏幕录制和麦克风权限，运维端需要授予麦克风权限。

### 6.5 强制控制（超级管理员）

```text
1. 超级管理员在设备列表对任一「在线」设备点击「强制控制」。
2. 运维网页调用 /api/device/force-control。
3. 服务端校验角色为 superadmin，确认设备在线且未被占用。
4. 后续与 6.3 相同：返回凭据 → 本机 /api/connect → 广播 control-started。
```

### 6.4 结束会话

- 网页主动结束：调用本地 `/api/disconnect`，再调用服务端 `/api/session/end`。
- GateDesk 主动退出：本地连接断开，网页通过 WebSocket 断开或状态轮询发现异常。
- 服务端检测到超时或异常断开：将会话置为 `ended`，并向对端发送 `peer-ended`。
- 会话结束后，服务端清理临时凭据、控制锁和 WebSocket 资源。

## 7. 安全与权限

### 7.1 受控端远程控制权限

GateDesk 基于 RustDesk 的远程桌面能力，将以下权限作为受控端的默认授权项。权限应绑定当前会话，并在会话结束、断开或凭据失效后立即撤销。

| 权限 | 说明 | 默认状态 |
|------|------|----------|
| 传输屏幕 | 向控制端实时传输受控端桌面画面，是远程协助的基础能力。 | 开启 |
| 控制鼠标键盘 | 允许控制端操作受控端的鼠标和键盘，执行点击、输入及系统操作。 | 开启 |
| 同步剪贴板 | 双向同步文字、链接等剪贴板内容，便于复制命令和信息。 | 开启 |
| 传输音频 | 将受控端系统声音传输给控制端，用于播放、视频或声音故障排查。 | 开启 |
| 复制粘贴文件 | 支持控制端与受控端之间复制、粘贴和传输文件。 | 开启 |
| 远程重启 | 允许控制端重启受控电脑，用于系统维护、更新或故障处理。 | 开启 |
| 录制会话 | 记录远程操作过程，用于审计、培训或问题复盘。启用前应告知用户并取得同意。 | 开启 |
| 阻止用户输入 | 临时禁用受控端本地鼠标和键盘，避免本地操作干扰远程处理。 | 开启 |
| 隐私模式 | 遮挡或隐藏受控端本地屏幕，避免现场人员看到远程操作和敏感信息；实际效果取决于操作系统及 RustDesk 配置。 | 开启 |
| 断开连接 | 结束当前远程会话，停止屏幕、音频、输入及其他数据传输。 | 开启 |
| 显示电脑信息、用户 ID、已连接时间 | 显示设备基本信息、用户身份标识和连接持续时间，用于设备识别、会话管理和审计。 | 开启 |

其中，“传输屏幕”是远程控制的核心能力；其他权限按会话授权使用。默认开启不代表控制端可无条件执行高风险操作：远程重启、录制会话、阻止用户输入和隐私模式等操作必须在界面中明确展示当前状态，并保留操作审计记录。

| 项目 | 设计要求 |
|------|----------|
| 本地监听 | 仅允许 `127.0.0.1`，端口可配置 |
| 本地鉴权 | 每次请求校验随机 Token，失败返回 `401` |
| Host 校验 | 拒绝非本机 Host，降低 DNS Rebinding 风险 |
| CORS | 仅放行 GateDesk 网页来源，禁止任意来源访问 |
| 凭据 | 服务端加密存储；仅按需短时下发；结束后失效或轮换 |
| 权限 | 只有运维角色可发起控制请求；受控端默认开启本节权限，但权限仅对当前会话生效，并对高风险操作进行明确提示和审计 |
| 防重放 | sessionId、控制票据和关键操作增加有效期及一次性校验 |
| 日志 | 不记录 Token、密码和完整敏感请求体；审计记录操作结果 |
| 限流 | 登录、注册、控制请求和聊天接口按用户及会话限流 |
| 输入校验 | 严格校验 ID、角色、sessionId、消息长度和 JSON 字段 |

## 8. 实施顺序

1. 在 GateDesk 客户端中实现本地 HTTP API、Token、端口配置和安全校验。
2. 打通 `/api/id`、`/api/status` 和 `/api/password`，完成客户端注册。
3. 重构 GateDesk 服务端，实现登录、配对、会话状态和 WebSocket 聊天。
4. 实现 `/api/connect`、`/api/disconnect` 及控制信令。
5. 实现用户网页、运维网页和启动脚本。
6. 完成双端同机、双机和断线场景联调。
7. 增加权限引导、审计、限流和临时凭据清理。
8. 验证完整闭环：登录 → 配对 → 聊天 → 控制 → 语音 → 结束。

## 9. 风险与待验证事项

- HTTP API 的实现方式需在“自写最小 HTTP 服务”和引入轻量依赖之间确定，并评估依赖维护成本。
- 需要验证现有 URI Scheme/IPC 入口在无界面常驻和多实例场景下的行为。
- 需要验证两个 GateDesk 实例在同一台机器上的配置、IPC、端口和进程隔离。
- macOS 屏幕录制和麦克风权限需要提供首次启动引导及失败提示。
- 浏览器地址栏、历史记录和代理日志可能暴露启动 Token，生产环境必须改为短时握手或一次性票据。
- 需要定义连接建立超时、心跳间隔、断线重连和会话清理策略。
- 需要明确“控制结束”与“会话结束”的区别，避免结束控制时误关闭聊天会话。
- 需要为控制请求增加并发控制，防止同一用户端被多个运维端同时接管。
